{% extends 'base.html.twig' %}

{% block title %}Modifier le mot{% endblock %}

{% block body %}
    <section class="w-50 m-auto">
        <h1>Modifier le mot</h1>

        {{ include('word/_form.html.twig', {'button_label': 'Modifier'}) }}

        <div class="btn-group">

            <a class="btn btn-primary" href="{{ path('word_index') }}">Retour à la liste</a>
    
            {{ include('word/_delete_form.html.twig') }}
            
        </div>
    </section>
{% endblock %}
